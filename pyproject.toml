[project]
name = "edge-agent-swarm"
version = "0.1.0"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["agents", "schemas", "pipeline"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
